# 拾链 · 迭代需求（2026-09-18）

> 这一轮要做什么、按什么顺序做、哪些不做。
>
> 事实基础：`server/`、`web/`、`sql/` 的代码逐行核对（**以代码为准，不以文档为准**——
> 文档已经有多处和代码对不上，见附录）。全部结论都带文件路径，可以自己去验。
>
> **实施状态（2026-09-18 晚更新）**：批次 1、批次 2 已实施（**R-09 除外**，见第五节）；
> 批次 3–6 未动。
>
> 原来放在工作目录里的三份配套材料（现状盘点 / 产品需求稿 / 技术评估稿）已按
> 「工程保持干净」的要求删除——它们的内容已经收敛进本文档，**不需要再去找它们**。

---

## 一、现在有什么

主流程已经闭环，能用了。

| 能力 | 状态 |
|---|---|
| 录入 → 抓取 → AI 分析 → 审核 → 入库 | 可用。含「分析过程可视化」「三句候选备注默认选第二句」「分析详情」 |
| 抓不到正文时贴正文重跑 / 事后给已存记录补正文 | 可用（`fix` 模式） |
| 列表、领域 / 用途筛选、客户端即时搜索、三种排序 | 可用 |
| 语义搜索（惰性补齐向量 + 余弦排序 + 低分兜底） | 可用，需另配向量服务 |
| 回顾（随机抽卡、一次一张、四个动作） | 可用，产品命门 |
| 周报（统计 + AI 摘要，按 ISO 周缓存 + 数据指纹作废） | 可用 |
| 账号（注册 / 登录 / 登出 / 改密）、多用户数据隔离 | 可用，已上线 |
| 安全底座（SSRF 防护、注册限流、失败锁定、bcrypt、会话固定防护） | 可用 |
| 韧性（熔断、退避重试、并发闸门）与可观测（traceId、分段耗时、`/api/health`） | 可用 |

**一句话诊断**：不是缺功能，是**功能做了半截**。三笔账：

1. **已有能力没接到界面上**——重复链接的 `existingId`、跳过 AI 的 `/api/links/quick`、书签小工具安装页，后端和静态页都做好了，用户碰不到。
2. **存进来的东西出不去也用不起来**——标签存了不能筛、数据不能导出、没有批量操作、移动端不可用。
3. **工程验证为零**——`server/src/test/` 不存在，改一处坏一处没人报警。而服务**已经上线**，回归靠手点的成本从「花自己的时间」变成「花别人的时间」。

---

## 二、迭代目标

把「做了半截」的兑现掉，把「已经上线才发现缺的」补上，并补上「开放给他人使用之后才需要」的管理能力。做完之后，用户从「主流程能跑通」变成「每条路都走得通」，而你从「只能开数据库」变成「有地方能管」。

排序只认三条信号：**正在生效的缺陷**最前（止损），**撞到就无解**其次（补洞），**后端已做完只差一个入口**再次（还债）。

**这一轮的规模要说清楚**：一共 20 条需求（P0 六条 / P1 十一条 / P2 三条）。其中「管理端 + 审计日志」（R-19 / R-20）是**单独一个量级**的工作——它不是画个页面，它引入了一个现在不存在的东西：**角色**。所以它排在最后一批，不阻塞其他任何需求；如果你想早点看到其他需求落地，完全可以先做完前五批再动它。

---

## 三、需求池

### P0（六条）

#### R-11 限流要取到真实客户端 IP

**描述**：让限流取到真实客户端 IP。现在它取不到——所有人被算成同一个 IP。

**结论：限流现在是失效的。**（2026-09-18 核实。**这推翻了本文档上一版的一个判断**——上一版我写的是「限流很可能一直正常」，那是错的。当时我只查到 `docker-compose.yml` 里写了这个变量，没查「配置里写了 ≠ 进程里生效了」。）

**证据链**（全部是硬证据，不是推断）：

1. `AuthController.java:266-268` 取 IP 用的是裸的 `request.getRemoteAddr()`。
2. `application.yml` 里**没有** `server.forward-headers-strategy`（只有 `server.port: 8080` 和 `server.address: 127.0.0.1`）。
3. 服务器上 `ps -eo pid,args` 显示的启动命令是
   `/opt/jdk21/bin/java -XX:MaxRAMPercentage=75 -Dfile.encoding=UTF-8 -Duser.timezone=Asia/Shanghai -jar /opt/shilian/shilian-server-0.1.0.jar`
   —— **没有** `-Dserver.forward-headers-strategy`。
4. `.env.properties` 里只有数据源、DeepSeek key、抓取代理三项，**没有**这个键。
5. Spring Boot 3.5.3 的 `ServerProperties.forwardHeadersStrategy` 字段**没有初始值**（默认 `null`）。字节码里 `TomcatWebServerFactoryCustomizer.getOrDeduceUseForwardHeaders()` 的逻辑是：策略为 `null` 时，返回「当前是否跑在云平台（Cloud Foundry / Heroku / K8s）」——阿里云 ECS 不是云平台 → **返回 false → 既不装 `RemoteIpValve`，也不装 `ForwardedHeaderFilter`**。（旁证：`server.tomcat.remoteip.remote-ip-header` 和 `protocol-header` 的默认值都是空，所以「不配策略、但配了 remoteip」这条备用路径也没走。）
6. 你在服务器上跑的 `grep -i FORWARD` 输出为空 —— 这条单独看不算铁证（也可能是命令没跑对），但第 2、3、4 条已经把其他可能都排除了。

**所以 `getRemoteAddr()` 对每个请求返回的都是 `127.0.0.1`（nginx 的地址）。**

| 限流 | 桶的 key | 额度（`application.yml:119,122`） | 现在的实际状态 |
|---|---|---|---|
| 注册 | `register:<IP>` | 5 次 / 小时 | **全站共用一个桶**——5 次用完，谁都注册不了 |
| 登录 | `login:<IP>` | 10 次 / 分钟 | **全站共用一个桶**——10 个人同时登录就有人被挡 |
| 注册（邮箱维度） | `register-email:<邮箱>` | 按天 | 不受影响 |

`AuthController.java:258-264` 的注释早就预言了这个后果，原话是「限流器直接变成拒绝服务工具」，末尾写着「上代理之前必须先做掉」。**代理上了，这条欠账现在正在生效。**

**修法——这里有个反直觉的点，别配错**：

正确的一行是 `SERVER_FORWARD_HEADERS_STRATEGY=native`，**不是 `framework`**。

原因是 nginx 那边（`web/nginx.conf:51`）用的是 `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`。`$proxy_add_x_forwarded_for` 是**追加**语义——客户端自己送来的 `X-Forwarded-For` 会原样保留在**前面**，nginx 只把真实地址追加在**最后**。于是这个头长这样：`<客户端伪造的值>, <真实客户端 IP>`。

- `framework`（Spring 的 `ForwardedHeaderFilter`）取**最左边**那个 → 拿到的是**伪造值** → 任何人加一个 `X-Forwarded-For: 1.2.3.4` 就能把限流整个绕过去，**根本不需要碰到 8080**。
- `native`（Tomcat 的 `RemoteIpValve`）是**从右往左扫、跳过可信代理、取第一个不可信的** → 拿到的是 nginx 追加在末尾的真实地址，**伪造值被自然跳过**。而且它只在「直连方是私有 / 回环地址」时才采信——`internalProxies` 默认正则已含 `127.\d+.\d+.\d+` 和 `::1`，nginx 从 `127.0.0.1` 连过来，正好命中。

**顺带一个要改的错**：`docker-compose.yml:38` 和 `DEPLOY.md:198` 里写的都是 `framework`。配上这个 nginx 配置，`framework` 恰好是**能被绕过**的那个选项。这两处要改成 `native`。

**验收标准**：

- 配置生效：写进 `application.yml`（随 jar 打包）即可，**不需要动启动脚本 / systemd**；`SERVER_FORWARD_HEADERS_STRATEGY` 无论是真实环境变量、还是 `.env.properties` 里的同名键，都能覆盖它（后者能生效的前提就是 yml 里有同名占位符，见下方「连带的坑」）
- 生效后：用**不存在的用户名**连打 11 次 `POST /api/auth/login` → 第 11 次得 429；**紧接着从服务器本机直连 `127.0.0.1:8080` 再打 1 次** → 不 429（两个来源落进了不同的桶，证明它认的是真实客户端 IP 而不是 `127.0.0.1`）
- 中途换一个伪造的 `X-Forwarded-For` 值，桶不变，仍然在第 11 次被拦

**为什么是 P0**：它是**正在生效的缺陷**——注册额度全站共用 5 次/小时，你集中邀请 9 个人时会撞到；登录 10 次/分钟，几个人同时登录就可能有人进不来。而修复是一行配置。

**关于原方案（自写 `ClientIpResolver`）**：架构评估给的「纯函数 + 显式可信列表 + 单测」是**更显式**的做法，但既然 Tomcat 的 `RemoteIpValve` 已经实现了同一个算法（右往左跳过可信代理），**这一轮先用配置解决，不写新代码**。理由：少一个自己维护的算法，就少一处会写错的地方；而 `internalProxies` 默认正则本身就是「只信私有 / 回环来源」这条安全属性。将来若换掉 Tomcat、或需要多级代理的精细控制，再回头做那个纯函数。

**已实施（2026-09-18，批次 1）**：

- `application.yml` 的 `server.forward-headers-strategy` 改成
  `${SERVER_FORWARD_HEADERS_STRATEGY:native}`（**写成占位符而不是字面量**，原因见下方「连带的坑」）。
  这一行随 jar 打包（`pom.xml` 的 resources 配置把 `src/main/resources` 整个打进去），
  所以宿主机 `java -jar` **直接生效，不依赖任何启动参数**。
- `docker-compose.yml` / `DEPLOY.md` / `web/nginx.conf` 注释里的 `framework` 一并改成 `native`。
- **实测验证过**（用当时临时立起的一套冒烟测试，3 个用例）：
  ① 换 IP 就换桶；② 只改伪造的最左值 → **仍被限流**（伪造值被忽略）；
  ③ 右值换成私网地址时它被跳过、身份落在左值上。
  并且**验证过那套测试有能力失败**——把配置注释掉后 3/3 全挂。一个「无论配没配对都绿」的测试等于没写。
  ⚠ **那套测试代码当天已按「工程保持干净」的要求删除**（连 `spring-boot-starter-test` 依赖一起），
  所以上面这几条现在是**手工验证的结论、没有回归测试兜着**。要重跑得先补回 R-09。
- 反编译 `tomcat-embed-core-10.1.42` 的 `RemoteIpValve.invoke` 确认取值规则：
  **从右往左扫、跳过命中 `internalProxies`（默认 = 私网 / 回环段，不止 127/10/172/192）或 `trustedProxies` 的项，取第一个不匹配的；
  只有直连方本身命中 internalProxies / trustedProxies 时才采信 XFF。**
  → 文档里那条「伪造最左值会被忽略」的核心断言**成立，不用改**。

**连带的坑（这一轮一起修了）**：`.env.properties` 里的大写+下划线键，**只有 `application.yml` 有同名 `${}` 占位符时才会被读到**。
Spring Boot 的 relaxed binding 只对**真实环境变量**做「大写 → 点号」的转换，`.properties` 文件走的是逐字比较。
写错形式**不报错、静默不生效**。`DEPLOY.md` 的部署模板里 7 个大写键有 4 个是空转的
（`SERVER_FORWARD_HEADERS_STRATEGY`、`SERVER_SERVLET_SESSION_COOKIE_SAME_SITE`、`SERVER_SERVLET_SESSION_COOKIE_SECURE`、`LOGGING_LEVEL_COM_SHILIAN`），
后三个已改成点号键——**其中两个是安全相关的（会话 Cookie 的 SameSite / Secure），你以为配了、其实一直没配。**

---

#### R-08 忘了密码能自救

**描述**：接通用 SMTP，做标准的「忘记密码 → 邮箱收链接 → 设新密码」自助流程。

**用户场景**：换设备的用户密码想不起来。原先的 `tools/user.sh` 能重置，2026-09-18 清理时删了。现在唯一的办法是手写 SQL 改 `app_user` 的密码列——**这件事你自己做得到，但你开放注册进来的那几个人做不到**。他们的唯一动作是来找你，而你的唯一动作是开数据库。

**验收标准**：未登录可发起；邮件里是带一次性 token 的链接（30 分钟过期、用后即废）；点开可设新密码并直接登录成功；**不泄露「该邮箱是否已注册」**（存在与不存在返回同一句话）；同一邮箱的发送有频率限制（防被当成发信机）。

**为什么是 P0**：这条的档位**因为你的一个回答而改变**。原稿把它放在 P1，理由是「穿哥手里有数据库，真无解不成立」——现在有 9 个人手里没有数据库。按 P0 的门槛（用户撞到就无解），它现在成立。

---

#### R-01 重复链接给出路

**描述**：保存撞到已存在的链接时，把后端 409 响应里的 `existingId` 用起来，给一个「去看看那条」按钮。

**用户场景**：粘网址回车 → AI 跑完 → 点保存 → 弹一行「已经存过了」——**然后就没了**。他不知道指的是哪条、当初给这条写过什么备注，只能回列表一条条翻，几百条时等于翻不到。

**验收标准**：409 时提示条带「去看看那条」；点击后列表滚动到该条、高亮 2 秒；被当前筛选挡住时先清筛选再定位。

> 「并展开」这一条去掉了（2026-09-18）。列表真正渲染的组件是 `LinkEntry`，它**本来就没有折叠态**——条目一直是展开的，没有「展开」这个动作可做。滚动定位 + 高亮已经覆盖了这条的本意（让用户看见那一条），再补一个折叠/展开交互就是给需求加戏。

**为什么是 P0**：后端做完了。`GlobalExceptionHandler.java:108-113` 的 409 响应体里就带着 `existingId`，`api.ts:21` 的注释甚至写着「前端可以直接跳过去」——前端没做。一个按钮加一次滚动定位，成本近乎零，而用户撞到的是一次「被拦下、没出路」的死胡同。

---

#### R-02 AI 失败时先把网址存下来

**描述**：分析失败（熔断 / 没配 Key / 超时）时，除「重新分析」「贴正文」外，加一条「先存网址，不分析」，调 `POST /api/links/quick`。

**用户场景**：没配 Key，或模型正在熔断。粘网址回车，转半天给个失败态，两个按钮**要么还得靠 AI、要么得打字**。他此刻只想先把网址存住，回头再补。没有第三条路——要么现在去翻正文贴进来，要么这个网址就丢了。

**验收标准**：失败态新增「先存网址，不分析」；成功后以「待补」进列表并带补入口；**熔断 / 未配 Key / 超时三种失败都能看到**，不是只有网络错才给。

**为什么是 P0**：`/api/links/quick` 后端已完成（`LinkController.java:112`），`REQUIREMENTS.md:99-111` 白纸黑字写着它的设计目的就是「AI 熔断时用户至少能把网址存下来」。功能做了、入口没做，而它踩在核心承诺上：丢一个 URL 进来，最差也得能存住。

**一个要顺手修的小缺陷**：补入口的文案写死是「正文没抓到，补上」（`LinkEntry.tsx:108`）。quick 存的记录不是「没抓到正文」，是**从没分析过**——文案会说错原因。修法是把 `analyze_status` 变成可读字段来区分两种待补。

---

#### R-03「已用」不再是一扇单向门

**描述**：任何一条记录都能事后标「已用」，也能取消。

**用户场景**：当初勾了用途「做项目」，两周后项目完了想标成已用。打开编辑面板——标题、摘要、标签、用途都能改，**就是没有一个「已用」开关**。而 `PATCH /api/links/{id}` 其实支持这个字段，前端只发过 `"read"`（`App.tsx:400`）。

**验收标准**：列表条目和编辑面板都能标 / 取消；已用条有视觉区分；标记后立即从回顾队列消失、侧栏计数同步；取消后回到队列。

**为什么是 P0**：直接顶在命门上。回顾的排除条件是「星标 / 已用 / 已读」三类出队，而「已用」只能在保存那一刻由用途决定一次——**队列会一直塞着用户早就用不上的东西**。写入口已在接口里，缺的只是前端一个开关。

**但这里有个必须先解决的语义坑**（见第四节决策一）。

---

#### R-04 回顾卡片能就地补正文

**描述**：回顾流程里，待补正文的卡片就地能补，不用退回列表。

**用户场景**：点进回顾抽到一张卡，卡片写着「回列表点『补上』」。他要么退出、去列表、找到这条、补正文、再回来——**节奏断成两截**；要么直接跳过——那这条大概永远补不上。

**验收标准**：待补卡片直接给「补正文」入口；点开能在卡片内粘贴正文并重跑分析，完成后**就地刷新**出新摘要；全程不离开回顾视图。

**为什么是 P0**：README 写着「这条如果做不好，前面所有功能都是白做的」。回顾里出现「必须离开才能解决」的提示，是产品自己打断自己。而补正文组件已经存在（`SavePanel.tsx:639-701` 的 `fix` 模式），是复用不是重写。

**它和 R-13（移动端）要合并做**（见第四节决策二）。

---

### P1（十一条）

| 编号 | 需求 | 一句话理由 |
|---|---|---|
| **R-05** | **标签能筛、也能管**（重命名 / 合并 / 删除） | 每存一个标签等于白存，是每天都在发生的小额损耗。筛选可继续走客户端（列表本来就要全量拉回来），但**标签管理必须上服务端**——它是跨行读改写 |
| **R-06** | **书签小工具给个入口** | 三条「已做好没接界面」里最弱的一条（不像 R-01/02 对应死胡同，仍能完成录入，只是多几步），但成本极低（一个链接），直接命中「四步变一步」 |
| **R-07** | **数据要有出口（导出）** | 对个人产品，「没有出口」不是效率问题，是**资产安全问题**。而且现在别人也往里存东西了，你更有义务给个出口。JSON（无损）+ 浏览器书签 HTML（能重新导入 Chrome/Edge） |
| **R-09** | **冒烟测试** | 见第四节决策三。它是这一轮所有高风险改动的地基，但自身不满足 P0 门槛。**2026-09-18 曾实现（15 个用例全绿），当天按「工程保持干净」的要求撤回——目前未实施** |
| **R-10** | **语义搜索失败能重试** | 现在失败只提示「可以关掉语义」——可用户的第一反应是「再来一次」 |
| **R-13** | **移动端回顾可用**（375px） | 你确认会在手机上翻回顾，所以本轮必做。但放 P1 不放 P0：移动端不可用不会让人**卡住**（桌面端一直在），它高的是使用频率，不是阻塞性 |
| **R-17** | **账号禁用生效（enforcement）** | 见第四节决策五。**必须本轮做完**——服务已上线、注册已开放，这个能力是「需要的那一刻必须已经存在」的，事后补来不及。它是 R-19 的地基 |
| **R-18** | **邮箱验证**（与 R-08 同批） | 和密码重置是**同一个原语**（证明你控制这个邮箱），token 生成 / 存储 / 校验 / 邮件模板全部共用。分开做等于把邮件服务的接入成本付两遍 |
| **R-15** | **AI 花销看得见**（从 P2 提到这里） | 你要求管理端的用量「详细一点」，而**详细的用量 = 按用户拆的 AI 花销**，这需要先把 `ai_log` 的记录点补全。所以它**必须排在 R-19 之前**——数字不全的管理端不如没有 |
| **R-19** | **管理端界面**（见下） | 你明确要做。**它是单独一个量级的活**，而且它引入了一个现在不存在的东西——角色 |
| **R-20** | **审计日志**（见下） | 你明确要做。和 R-19 同批，因为它服务的是同一个问题：出事了要能追溯 |

---

#### R-15 AI 花销看得见（顺序被你的一个回答改了）

**描述**：把每一次 AI 调用都记进 `ai_log`，并按用户聚合成「谁花了多少」。

**用户场景**：现在有别人在花你的钱。`/api/links/{id}/analyze` 和 `/reanalyze` **本身不入账**——最容易失控的浪费路径（反复重分析、最后没保存）在账上是隐形的。你想知道「9 个人里谁在用、用了多少」，现在没有任何数据能回答。

**验收标准**：`/analyze` 与 `/reanalyze` 的每次调用（含失败）都落 `ai_log`；能按用户、按时间段查出调用次数与 token 合计；管理端展示这个数字。

**好消息是表已经就绪**：`ai_log` 有 `user_id`（`V1__baseline.sql:179`）、`prompt_tokens` / `completion_tokens`（`:182-183`），还带 `idx_ai_log_user (user_id, created_at)` 索引（`:188`）——**缺的只是记录点，不是表结构。**

**为什么从 P2 升到 P1**：它的触发条件原本写的是「月账单开始起伏就该做」，你这次要求管理端显示详细用量，等于手动触发了它。**而且顺序是硬的：R-19 要显示的数字来自 R-15，反过来做会得到一个数字不全的管理端，而错的数字比没有数字更糟。**

---

#### R-19 管理端界面

**描述**：一个只有管理员能进的管理页：看全部用户、搜索、分页、禁用 / 恢复、看每个账号的用量。

**用户场景**：某个账号不再需要用了，你要收回；或者某天发现有人在灌垃圾链接，你要找到那个账号并立刻停掉它。现在这两件事你都只能开数据库、手写 SQL——而 `app_user.status` 就算改了也不生效（见 R-17）。

**验收标准**：一页能看到所有用户（邮箱、昵称、注册时间、最后登录、状态、链接数、**AI 调用次数与 token 合计**）；可按邮箱 / 昵称搜索、可分页；能一键禁用 / 恢复；**非管理员访问管理接口返回 404 而不是 403**（和现有约定一致：403 等于确认资源存在）。

**用量那一列依赖 R-15**（你这次要求「详细一点」，等于把它从 P2 提到了 P1）——它必须排在 R-19 之前，否则这一列要么是空的、要么是错的。

**为什么是 P1**：它不阻塞任何人用产品，所以不是 P0。但它是「需要的那一刻必须已经存在」的能力。

**这条的真实起点不是画页面，是「角色」**——现在全系统所有账号是平等的，没有管理员这个概念。所以顺序是：

1. `app_user` 加 `role` 列（或等价的布尔标记），并**定下谁来做第一个管理员**
2. 后端管理接口做鉴权（只有管理员能调）
3. 前端路由守卫 + 管理入口（非管理员看不见）
4. 页面本身

**第 1 步已经有答案了**（见第六节 Q7）——第一个管理员是你自己（`chuange`），用一条显式 SQL 设定，**不做「第一个注册的人自动成为管理员」这种隐式规则**。具体语句见第六节。

---

#### R-20 审计日志

**描述**：记录「谁在什么时候做了什么」，并且能查。

**用户场景**：某天某个账号的数据没了，你想知道是他自己删的、还是被别人删的；或者你想确认某个账号是什么时候注册进来的。现在这些问题**没有任何数据能回答**——出事了只能靠猜。

**验收标准**：登录成功 / 登录失败、改密码、重置密码、删除链接、越权尝试，都落一条记录（谁、何时、做了什么、结果、来源 IP）；新表 `audit_log`；管理端能按用户 / 时间 / 动作查。

**为什么是 P1**：它和 R-19 是同一个问题的两半——管理端解决「我能动手」，审计日志解决「我知道发生了什么」。所以同批做。**但要注意它有个隐形成本**：记录点要埋在多个地方（认证、改密、删除），漏一处就是一段查不到的空白，而漏了不会报错。

**依赖**：`REQUIREMENTS.md` 的 C15 早就把它列为 P1，当时的理由是「多用户产品必须有」；现在你的产品确实多用户了。

---

### P2（三条，都有明确触发条件）

| 编号 | 需求 | 触发条件 |
|---|---|---|
| **R-12** | 量级性能改造：`LIKE` → `FULLTEXT` + ngram 分词器；`ORDER BY RAND()` → 分桶随机；暴力余弦 → ANN | 前两项库到几千条，语义到几万条。**现在是个位数规模，几千条之前代价肉眼不可见**。但触发线要写进代码注释，别等真卡了再回来找原因 |
| **R-14** | 批量操作（多选、批量删除 / 改用途 / 加标签） | 条目上百、且开始有「整批整理」的需求。个位数条目时，批量省下的时间是零。**管理端里的批量是另一回事**，见 R-19 |
| **R-16** | 汇总视图（领域 / 用途 / 标签的分布） | 库大了；且要先补一个返回计数的接口 |

---

## 四、六个关键设计决策

这几条是这一轮真正需要想清楚的地方，其余都是执行。

### 决策一：「已用」要拆成独立列

现在 `status` 是一列枚举 `{unread, read, used}`。问题在于——**「已读」（永久别再推）和「已用」（可撤销）是两件正交的事**，挤进一列之后，「取消已用」没法知道它原来是 `unread` 还是 `read`：

- 一律回 `unread` → 一条你标过「看过了」的记录会**错误地冒回回顾队列**；
- 不允许对 `read` 记录标已用 → 又违背「任何一条都能标」。

所以借这次迁移把 `used` 拆成独立列，`status` 只留 `{unread, read}`，出队条件变成 `starred=1 OR status='read' OR used=1`。状态机因此全定义：

```
unread  ──标已用──► used        取消 ──► unread  （回队列，正确：它本来就没被读过）
read    ──标已用──► read+used   取消 ──► read    （不回队列，正确：read 是永久的）
```

**不选**「维持单列 + 只允许在 unread 上标已用」：那是给根因打补丁，留一个以后一定会被踩的雷，而这一轮本来就要做迁移。

### 决策二：R-04 和 R-13 合并做

两条都要重写 `ReviewView.tsx` 的同一块布局。**更硬的理由**：R-04 要加的「内嵌编辑器」恰恰是 375px 下最容易撑破的部分（textarea + 按钮 + 备注候选 + 分类 chips）。先按 468px 抽屉做完再适配窄屏，等于把适配重做一遍——**编辑器必须在做响应式那一遍时已经存在**。

合并的收获是一条约束：抽出来的 `DraftEditor` 会被两个宽度迥异的宿主共用（抽屉固定 `w-[468px]`、回顾卡片约 375px），所以它**必须是宽度无关的流式组件**，宽度由宿主外壳决定。这个约束在做 R-04 抽取时就要写死，否则会抽出一个隐含 468px 假设的组件，到 R-13 再回头拆。

### 决策三：测试必须连真实 MySQL

工程只支持 MySQL，本地开发和部署**同一份配置、同一条代码路径**，没有 profile 切换、没有内存库。所以测试不能假装用 H2——那会验出「本地全绿、线上全炸」。

两个可落地方案：

- **方案 A（推荐）**：Testcontainers 起一次性 MySQL。真 MySQL、同一个驱动、同一套 SQL、同一条代码路径，每次全新 schema，**天然可重复、天然不污染真实数据**。代价：需要本机有 Docker。
- **方案 B**：连配置里的真库，按用户隔离（每个用例注册随机账号，仓储层强制 `user_id` 过滤，`@AfterEach` 清干净）。风险：`claimOrphans`（首个账号认领历史行）会污染真实数据，该路径不测；teardown 出 bug 会留垃圾。

AI 调用一律用 `LlmPort` / `FetcherPort` 的测试替身，**绝不真调 DeepSeek**——「周报降级返回」这个用例正是靠「让 `LlmPort` 抛异常」来构造。

**「一条命令跑完」这个验收标准要改写**：它冲突于天真的读法——干净机器上跑不起来。正确表述是「**一条命令，前提 MySQL（或 Docker）在跑**」。工程已有的 `StartupSelfCheck` 和 `DatabaseUnreachableFailureAnalyzer` 可以复用，让「库没起」失败得清楚而不是抛一堆栈。

### 决策四：邮箱规范化是伪问题（推翻了文档结论）

`REQUIREMENTS.md:276-279` 写着「邮箱原样存储、精确匹配，`A@x.com` 和 `a@x.com` 会被当成两个账号」。**这与 DDL 矛盾**：

`app_user` 表的 `COLLATE=utf8mb4_unicode_ci`（`V1__baseline.sql:121`），而 `email VARCHAR(190)`（`:105`）**没有覆盖 collation**，于是列继承 `ci`——**`_ci` 就是大小写不敏感**。推论：`WHERE email = 'a@x.com'` 会命中 `A@x.com`；唯一索引 `uk_app_user_email` 同样不区分大小写，**插不进第二条大小写变体**。

也就是说「一个人占两个号」在存储层**已经不可能**，不存在需要合并的重复行，**不需要专项迁移**。只做两件小事：在生产库跑一条 `SHOW FULL COLUMNS FROM app_user LIKE 'email'` 确认 collation；把存量邮箱统一小写一次（为展示整齐，并入 V3 顺手做）。

**顺手要把 `REQUIREMENTS.md` 那条错误结论改掉**——它会让后来的人按错误前提动手。

### 决策五：管理能力要做全，但先认清它的起点是「角色」

**先说现状为什么等于没有**：`app_user.status` 写了 `'active'` 但**全工程零校验**，`User.java:26` 的 `STATUS_DISABLED` 零引用。就算手写 SQL 把 status 改成 disabled，登录逻辑也不读它，人照样能进。

**关键实现点**：`SecurityContextCurrentUser`（`:35-44`）只从会话里的 `ShilianPrincipal` 取 `userId`，**不查库**——所以**光在登录时校验 status 不够**，一个已被禁用的用户只要会话没过期就继续能用。需要**已认证请求上每请求校验一次 status**。登录时也校验（给明确提示，但不区分「密码错」和「被禁用」，避免暴露账号状态）。

**然后才是管理端**（R-19 / R-20）。这两条的真正难点不在页面，在于它们要求系统第一次有「**谁能做什么**」的概念：

- 现在所有账号完全平等，没有管理员。要做管理端，必须先有 `role`（或等价的标记）
- 后端每个管理接口都要鉴权——**而且越权要返回 404 而不是 403**（沿用现有约定：403 等于确认资源存在）
- 前端要有路由守卫，非管理员看不见入口
- 「谁是第一个管理员」需要人工定一次（见 Q7）

**所以这一条的量级要提前知道**：它不是「画个用户列表」，它是「给系统加上角色这个概念，再在上面搭一个页面」。这也是为什么它排最后一批——**它不阻塞其他任何需求**。

### 决策六：`site_name` 不是死列，是断了一截的链路

盘点时它被标成「永远是 NULL 的死列」，但查下去发现：`ExtractService.java:45-57` **已经提取了** `siteName`，而 `AnalyzeDraft` 没有这个字段无处承接，`LinkController.java:66` 硬编码传 `null`。

所以修法是**补链路**而不是删列：`AnalyzeDraft` 加 `siteName` → `AnalyzeService` 填 → `LinkController.save` 传（`NewLink.siteName` 字段已存在）。

**同一批要处理的死列判决**：

| 列 | 判决 |
|---|---|
| `link.content_hash` | **删**（完全死列，无读无写） |
| `link.is_private` | **删**（半实现不可达；语义已由 `needs_review` + `analyze_status` 表达） |
| `link.site_name` | **补链路** |
| `link.analyze_status` | **补读者**（配合 R-02 区分两种待补） |
| `link.summary_long` | **留**（用于语义索引 + 关键词 `LIKE`，是活列） |
| `app_user.status` | **留，并补 enforcement** |
| `app_user.last_login_at` | **留**（R-19 的管理端正好要展示「最后登录」，它从死列变成了活列） |
| `app_user.role` | **新增**（R-19 需要，见决策五） |

---

## 五、实施顺序

分六批。原则：**一批做完就能用起来，不出现「做了一半、产品更差」的中间态**；每批可独立交付。

```
批次1 R-11 ──────────────────┐
                              ├──► 批次3 回顾收口 ──┐
批次2 断头路 + 工程地基 ───────┘  R-03/04/13 + V2   │
  R-01/02/06/10 + R-09 + 文档                        ├──► 批次5 标签 + 出口
                              ┌──────────────────────┤    R-05 + R-07
批次4 账号安全补全 ────────────┘                      │
  R-08 + R-18 + R-17 + V3                             │
                                                      ▼
                                          批次6 用量 + 管理端 + 审计日志
                                             R-15 + R-19 + R-20 + V4
```

**批次 1｜R-11：限流按真实客户端 IP**　✅ 已实施（2026-09-18）
*实际做法比原计划简单*：**没写 `ClientIpResolver`**——直接用一行配置
`forward-headers-strategy: ${SERVER_FORWARD_HEADERS_STRATEGY:native}`（`application.yml`）。
Tomcat 的 `RemoteIpValve` 已经实现了「从右往左跳过可信代理」这个算法，自己再写一遍只是多一处会写错的地方。
*验收*：伪造的转发头绕不过限流、受信代理的真实 IP 正确分桶（**已实测**）；
`docker-compose.yml` / `DEPLOY.md` / `web/nginx.conf` 注释里的 `framework` 一并改成 `native`。
（受信来源用的是 Tomcat 默认正则——所有私网 / 回环段，比只信 `127.0.0.1` 宽；安全性挂在
`server.address: 127.0.0.1` 上，约束已写进 `application.yml` 的注释。）

**批次 2｜接上断头路 + 工程地基**　✅ 已实施（2026-09-18），**但 R-09 除外**
R-01 + R-02 + R-06 + R-10（纯前端小改动）+ 文档纠错 8 处。
*验收*：409 → 定位 + 高亮 + 清筛选；失败态 → 「先存网址，不分析」入库并定位；
账号菜单 → 书签小工具；语义失败可原地重试；`cd web && npm run build` 零错误。

> **R-09（测试基座）曾实现，当天撤回。** 2026-09-18 立起过一套冒烟基座
> （`server/src/test/java/com/shilian/smoke/`，15 个用例全绿，连真实 MySQL 的独立 `_test` schema），
> 随后按「工程保持干净」的要求**连同 `spring-boot-starter-test` 依赖一起删除**，
> 测试在生产机上建的 `shilian_test` 库也已清掉。**所以工程现在仍然没有任何自动化测试**，
> 批次 3 的「要有测试兜住重构和迁移」这个前提**不再成立**——要么先补回 R-09，要么接受裸奔。
> R-09 作为需求仍然成立，只是回到「未实施」。

**批次 3｜回顾流程收口（重构 + V2 迁移，本轮风险最高）**
R-03 + R-04 + R-13 + V2 迁移。**原计划排在批次 2 之后、靠 R-09 的测试兜住重构和迁移——这个前提已经没有了**（见上）。
*所以开工前先做一个选择*：要么先把 R-09 补回来（半天量级，成本已探明），要么明确接受这一批裸奔改。
*验收*：标 / 取消「已用」后队列与计数自动同步（`unread` 回队列、`read` 不回）；回顾卡片就地补正文、全程不离开视图；375px 宽下回顾流程完全可用；V2 在新库（只跑 V1）与老库（V1+V2）上结构一致、可重复跑、无数据丢失。

**批次 4｜账号安全补全（邮件 + 验证 + 禁用）**
R-08 + R-18 + R-17 + V3 迁移（`email_verified` 列 + `password_reset_token` 表 + 存量用户 grandfather + 存量邮箱统一小写）。
*验收*：忘密码走邮箱链接、一次性 token、30 分钟过期、用后即废；`forgot` 对存在 / 不存在的邮箱返回同一句话；账号被禁用后**新登录与已有会话的下一请求都被拒**。

**批次 5｜标签能筛能管 + 数据出口**
R-05 + R-07。
*验收*：顶栏标签 chips 多选带条数即筛；重命名 / 合并 / 删除生效、合并后不产生重复；导出 JSON + HTML 按用户隔离、几百条不卡。

**批次 6｜用量入账 + 管理端 + 审计日志（单独一个量级，排最后）**
R-15 + R-19 + R-20 + V4 迁移（`app_user.role` 列 + `audit_log` 表 + 首个管理员的人工设定）。
*为什么排最后*：它不阻塞任何其他需求；而它自己依赖批次 4 的 R-17（禁用 enforcement）——管理端要能禁用，得先让禁用真的生效。
*为什么 R-15 排在本批最前*：管理端要显示的「详细用量」这个数字来自 R-15。**先做页面后补账，页面就是错的。**
*验收*：非管理员访问管理接口得 404；管理员能看到全部用户、搜索、分页、禁用 / 恢复、**每个账号的 AI 调用次数与 token 合计**；登录成功 / 失败、改密、重置、删链接、越权尝试都落审计记录，且能在管理端查到。

---

## 六、需要你拍板 / 确认的

### 已确认（2026-09-18）

- **Q5 部署形态**：nginx 用 Docker 部署，后端是宿主机上跑的 `java -jar`
  → **R-11 的受信代理地址填 `127.0.0.1`**（不是 docker 网段）。
- **Q6 8080 是否对公网开放**：**没有**，只有 nginx 映射了
  → 「伪造转发头绕过限流」这条路堵着，最坏的可能排除。
- **Q1 邮件服务**：**通用 SMTP，不锁厂商**。配置里填 host / port / user / password。
  要提前确认一件事：有些服务商的免费额度**只允许发给验证过的邮箱**，这会影响能不能发给那 9 个人。
- **Q2 邮箱验证**：**不强制**。注册后即可用，验证只作为找回密码的前置。
  硬规则：**没验证邮箱的账号不能走重置流程**。
- **Q3 管理能力**：**做全**——管理端 + 审计日志（R-19 / R-20）。已排进批次 6。
- **Q4 邮箱规范化**：**不做专项迁移**（见决策四）。只确认 collation + 存量邮箱统一小写一次。
- **Q7 第一个管理员**：**你自己**（`chuange`），用一条显式 SQL 设定，**不做「第一个注册的人自动成为管理员」这种隐式规则**。语句见下。
- **Q8 管理端的用量**：**要详细**——按用户看 AI 调用次数与 token 合计。这等于把 R-15 从 P2 提到 P1，并排在 R-19 之前。

### 第一个管理员的语句（可以直接跑）

⚠ 先说实话：**现在跑下去不会有任何可见效果**——`role` 这一列目前还不存在，而且没有任何代码读它。它的作用是为 R-19 铺路：等管理端做出来时，你就是那个管理员。现在跑（幂等、可重复执行）或者留到批次 6 一起做，都行。

```sql
-- 1) 加 role 列。MySQL 没有 ADD COLUMN IF NOT EXISTS，所以先查一次，保证可重复跑
SET @has_col := (SELECT COUNT(*) FROM information_schema.COLUMNS
                 WHERE TABLE_SCHEMA = DATABASE()
                   AND TABLE_NAME = 'app_user'
                   AND COLUMN_NAME = 'role');
SET @ddl := IF(@has_col = 0,
  'ALTER TABLE app_user ADD COLUMN role VARCHAR(16) NOT NULL DEFAULT ''user'' AFTER status',
  'DO 0');
PREPARE stmt FROM @ddl; EXECUTE stmt; DEALLOCATE PREPARE stmt;

-- 2) 设成管理员。这里必须是 UPDATE、不是 INSERT——
--    chuange 这个账号已经存在，再 INSERT 会撞 uk_app_user_username 唯一索引
UPDATE app_user SET role = 'admin' WHERE username = 'chuange';

-- 3) 核对
SELECT id, username, email, role, status FROM app_user;
```

**这就是 V4 迁移的第一段。** 批次 6 做 R-19 时把它原样搬进 `V4__admin.sql`，再补 `audit_log` 建表。
两份 DDL 的纪律不变：**V1 基线不动**（它是给全新库用的，改了等于让新库和老库的结构分叉），
新的变更一律走新迁移文件；`sql/schema.sql` 作为「最终态」那一份，在批次 6 落地时同步补上。

（执行方式：`mysql` 命令行或图形客户端整段跑即可。如果走 JDBC 执行器，记得它一次只吃一条语句，
要开 `allowMultiQueries`——这个坑本项目踩过。）

### 本轮问题已全部确认

Q1–Q8 都有答案了，这份需求可以定稿开工。唯一还悬着的是 R-11 那行配置（`native`）什么时候加——
建议**尽快**，理由见该条：它现在是全站共用一个限流桶。

---

## 七、明确不做的

- **CSRF 开启**。前端还没有取 XSRF token 的逻辑，现在开启会让所有 POST 直接 403，主流程全线瘫。当前 `SameSite=Lax` 已缓解大部分场景。
- **DNS rebinding 彻底防护**。要在 socket 层绑定校验过的 IP、处理 SNI 与证书，代价和收益不成比例。
- **改密码踢其他设备**。需要 `SessionRegistry`，而当前登录是手动调 `authenticate`、不走登记。**但如果哪天出现账号被盗，它会从「不做」变成「必要」**——而你已经有 R-17 的禁用能力可以应急。
- **向量库 / ANN 索引**。见 R-12，几万条之前暴力余弦够用。
- **Prometheus / OpenTelemetry**。现在只有日志 + traceId，能定位「这一次请求」就够。**审计日志不是可观测性**——它回答「谁做了什么」，不回答「系统现在什么状态」，别拿它替代监控。
- **单条分享**。现在有其他用户了，「分享给谁」不再是空问题——但直接发原网址即可，仍不到必要。

---

## 附录：顺手要修的文档错误（**已在批次 2 全部修完**）

**文档和代码脱节得比较厉害**，而且有一处的错误结论会导致后来的人按错前提动手。
下面这张表是当时的清单，**已全部修掉**，留作记录。

> ⚠ 表里的行号是**修之前的**，改完之后行号会漂，照着行号去找会找错——
> 要核实就用「当时写着」那一列的文字去搜。

| 位置 | 当时写着 | 实际是 |
|---|---|---|
| `server/README.md` | 「用 Spring JDBC 而不是 MyBatis-Plus」 | **已全量迁移 MyBatis-Plus 3.5.17**（`repo/entity/` 6 个 + `repo/mapper/` 6 个 + `repo/handler/` 2 个；`LinkRepository` 类注释写明了迁移日期） |
| 根 `README.md` | 「没有版本控制，不在 git 里」 | **已有 git**，提交 `61c0208`，138 个跟踪文件 |
| `server/REQUIREMENTS.md` | 「B12 已完成」（跳过 AI 保存） | 后端完成，**前端当时无入口**（就是 R-02，批次 2 已接上） |
| `server/REQUIREMENTS.md` | 引用 `RateLimiterTest.reportedWaitIsActuallyEnough` | **该测试不存在**（`src/test/` 早已删） |
| `server/README.md` | 目录树写 `CorrectionsSource` | 实际类名是 `CorrectionsFeed` |
| `server/REQUIREMENTS.md` | 「邮箱原样存储、精确匹配，大小写算两个账号」 | **与 DDL 矛盾**（列继承 `utf8mb4_unicode_ci`，大小写不敏感），见决策四 |
| `docker-compose.yml` / `DEPLOY.md` | `SERVER_FORWARD_HEADERS_STRATEGY=framework` | **该用 `native`**。nginx 用的是 `$proxy_add_x_forwarded_for`（追加语义），`framework` 取最左值 = 客户端可伪造，能被绕过。见 R-11 |
| `application.yml` | 注释「个人自用的工具不需要对外开放注册」 | **已经开放注册了**（服务已上线、有真实用户） |

**另外一并修掉、但不在上表里的**：`server/README.md` 的「没有任何自动化验证」一节和
`server/REQUIREMENTS.md` 的「没有自动化测试」一段，在批次 2 里被 R-09 的冒烟基座改成了
「已有测试」；R-09 撤回之后又改了回来——**现在两处都说的是「没有任何自动化测试」**，
并都指向 R-09 作为待补项。

### 迁移写法要点（原先记在技术评估稿里，那份稿已删，在这里重记一遍）

实现 V2 / V3 / V4 时照做：

- MySQL 8.0 **不支持** `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`。幂等要靠
  `information_schema.COLUMNS` 先查存在性，再用 `PREPARE` + `EXECUTE` 执行动态 DDL
  （`DO 0` 作为「已经有了」时的空操作分支）。
- **DDL 隐式提交、不能回滚**，所以每条迁移都要能「跑第二遍不报错、且结果不变」。
- **新迁移在新库上必须是无害 no-op**：全新库会先跑 V1 建出完整结构，V2 再跑一遍时要认出
  「已经有了」直接跳过。
- **两份 DDL 都要改**：`sql/schema.sql`（部署时手工执行）和
  `server/src/main/resources/db/V1__baseline.sql`（应用从 **classpath** 读它建表）。
  **两者之间没有同步机制**，改一个必须记得改另一个。
- 版本记录在 `schema_version` 表（不用 Flyway）。
- 迁移由 `Migrations` 在应用启动时执行——**也就是说写完 V2，下次重启生产服务就会真的改生产库结构。**

---

*文档结束。一句话总结：这一轮不是加功能，是把**做了半截的做完**——接口都在，缺的是入口；能力都在，缺的是出口；改动都在，缺的是兜底。而最后一批（管理端 + 审计日志）是唯一一件「从零加一个概念」的事，所以它单独排、单独验，不拖累前面。*
